<Window x:Class="YTubeFetch.Views.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About YTubeFetch"
        Width="350" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}"
        Icon="/Resources/YTubeFetch.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="YTubeFetch" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"
                   Foreground="{DynamicResource TextPrimaryBrush}"/>
        <TextBlock Grid.Row="1" Text="Version 1.0.2" Margin="0,0,0,8"
                   Foreground="{DynamicResource TextPrimaryBrush}"/>
        <TextBlock Grid.Row="2" Text="Windows GUI YouTube Downloader" TextWrapping="Wrap"
                   Margin="0,0,0,8" Foreground="{DynamicResource TextSecondaryBrush}"/>

        <Button Grid.Row="4" Content="OK" Width="80" HorizontalAlignment="Right"
                IsDefault="True" IsCancel="True"/>
    </Grid>
</Window>
